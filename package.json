{
  "name": "vad-cut",
  "version": "1.0.0",
  "description": "课程视频掐头去尾剪辑工具 - 基于VAD自动识别有效内容边界",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "setup": "node scripts/download-model.js",
    "build": "electron-builder --win --config electron-builder.json"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "sherpa-onnx": "^1.10.30"
  },
  "devDependencies": {
    "electron": "^33.4.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
